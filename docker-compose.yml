version: '3.8'

services:
  frontend:
    build:
      context: .     
      dockerfile: Dockerfile
    ports:
      - "5050:5050"             
    environment:
      - NODE_ENV=production     
    # volumes:
    #   - ./:/app       